# Proxy URL (http:// or socks5://). Leave empty for direct mode (UDP blocking only).
# Default to local Tor SOCKS (installed via package deps).
PROXY_URL=socks5://127.0.0.1:9050

# Optional list of proxies to try (first reachable wins) when PROXY_URL is empty/unreachable.
# Example: PROXY_CANDIDATES="socks5://127.0.0.1:9050 http://1.2.3.4:8080"
PROXY_CANDIDATES=""

# Disable non-proxied UDP so WebRTC uses TCP/TURN instead of direct UDP.
DISABLE_NON_PROXIED_UDP=1

# Extra Chromium/Electron flags to append, space-separated.
#EXTRA_FLAGS="--disable-gpu"
